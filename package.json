{
  "name": "privacy-safe-web-qa-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Reproducible browser and API QA checks with privacy-safe evidence",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "playwright": "1.62.1"
  }
}
